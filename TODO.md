# TODO

Отложенные и крупные темы. Не смешивать с текущим эпиком генерации картинок.

## Биллинг платных функций (отложено)

Генерация изображений через OpenRouter (Flux) платная. Полноценный биллинг **не** входит в первый релиз `generate_image`: там достаточно суточного лимита и лога стоимости.

Когда дойдём до биллинга — отдельный эпик:

- Баланс пользователя и стартовый грант при регистрации (email и OAuth).
- Append-only таблица списаний/начислений (`ledger`): сумма, причина (`signup_grant`, `image_generation`, …), ссылка на операцию, время.
- Списывать **после успешного** ответа провайдера, по фактической стоимости (`usage.cost` OpenRouter → внутренние центы), не «на глаз» до запроса.
- Идемпотентность (повтор того же `tool_call_id` / generation id не списывает дважды).
- Нехватка средств: tool не вызывает OpenRouter, пользователю — понятная ошибка.
- UI баланса — после модели данных, не вместо неё.

Пока биллинга нет, в `AGENTS.md` остаётся факт «биллинга в MVP нет». Строку менять только вместе с этим эпиком.

Не делать в том же PR, что `generate_image` + S3.
